[pytest]
testpaths = tests
# rootdir on sys.path, so tests import `app` and each other's helpers without
# the package needing to be installed.
pythonpath = . tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:app.*
